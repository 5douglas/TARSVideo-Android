# Archived documentation

This directory preserves documentation from older TARSVideo Android
experiments and releases.

Files here are historical references and should not be interpreted as current
build instructions.

For current documentation, use the files in the parent `docs/` directory and
the repository root `README.md`.
